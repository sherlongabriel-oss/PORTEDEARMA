services:
  - type: web
    name: 357-bot
    env: node
    plan: starter
    buildCommand: npm install ; npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 3000
